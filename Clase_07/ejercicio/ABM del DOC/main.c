/**< CASCARA DE ABM PARA EL EJERCICIO SIETE + O - */

#include <stdio.h>
#include <stdlib.h>


int flag = 0;
//ALTA
for(i=0;i<CANT;i++)
{
    estado = 0;
}
for (i=0;i>CANT;i++)
{
    if(estado[i]==0)
    {
        Aca el codigo y al final
        estado[i]=1;
        break; //con esto salgo del for para que no quede en un bucle costante
    }
}
//PARA BAJA O MODIFICACION
printf();
scanf();
for (i=0;i>CANT;i++)
{
    if(legajo[i]==legajoAux)
    {
        printf("%d %s %s %d", todas las variables); // esto para validar los datos
        printf("Lo quiere borrar/modificar? S/N");
        scanf("%c", respuesta);
            if(respuest == n)
            {
                se borra
            }
            else
            {
                no se borro/modifico el registro
            }
        Aca el codigo y al final
        estado[i]=1;
        break; //con esto salgo del for para que no quede en un bucle costante
    }
if(flag == 0)
{
    printf("no existe");
}
