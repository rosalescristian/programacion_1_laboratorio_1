
int funcion_menu(int salida); // funcion_menu tiene la cascara del menu con las opciones para el ABM, dispara las funciones listar y devuelve el cierre del programa
int get_int(opcion, opcion_imput);//valida el int; no funciona bien.
void modificar_socio(e_socio vec[], int CANTIDAD);
void mostrar_socio(e_socio lista);
int buscar_socio(e_socio vec[], int CANTIDAD, int codigo_socio);

